{
  "name": "invisiblequantam",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": "20 || 22 || 24"
  },
  "scripts": {
    "build": "shx rm -rf dist && shx mkdir -p dist && copyfiles -u 0 *.html css/**/* js/**/* images/**/* fonts/**/* documents/**/* videos/**/* dist",
    "preview": "npm run build && http-server dist -p 4173 -c-1",
    "serve": "npm run build && firebase emulators:start --only hosting",
    "deploy": "npm run build && firebase deploy --only hosting",
    "hosting:channel": "firebase hosting:channel:deploy preview"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "firebase-tools": "^14.26.0",
    "http-server": "^14.1.1",
    "shx": "^0.4.0"
  }
}